<% provide(:title, '店舗一覧') %>

<div class="content-wrapper">
  <div class="shop-index-wrapper">
    <h1>店舗一覧</h1>
    <ul class="shops">
      <%= render partial: 'shop', collection: @shops, as: 'shop' %>
    </ul>
    <%= paginate @shops %>
  </div>
</div>
