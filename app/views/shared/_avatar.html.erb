<% if user.avatar.attached? %>
  <%= image_tag user.avatar.variant(resize_to_limit: [200, 200]).processed, class: "avatar" %>
<% else %>
  <%= image_tag "avatar_default.png", alt: "アイコン", class: "avatar", id: "avatar-default" %>
<% end %>